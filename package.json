{
  "name": "nexi-etsy-api",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Secure Etsy Open API v3 OAuth/PKCE bridge for Nexi.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server-v2.js",
    "dev": "node --watch src/server-v2.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}
